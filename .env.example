# OpenRouter API Configuration
OPENROUTER_API_KEY=your_openrouter_api_key_here
# LiteLLM expects OPENROUTER_KEY (not OPENROUTER_API_KEY!)
OPENROUTER_KEY=${OPENROUTER_API_KEY}
# Optional: Override default model
# OPENROUTER_MODEL=anthropic/claude-sonnet-4
OPENROUTER_MODEL=google/gemini-3-pro-preview
# OPENROUTER_MODEL=openai/gpt-oss-120b:free
# ============================================================================
# Data Source Configuration
# ============================================================================

# Database Directory (DrugBank, BindingDB, Pharos, GWAS, StringDB)
# Default: /home.galaxy4/sumin/project/aisci/Competition_Data
DATABASE_DIR=/home.galaxy4/sumin/project/aisci/Competition_Data

# Input Data Directory (question-specific data files)
# Default: ./data
INPUT_DIR=./data/Q5

# Paper Library Directory (local PDF collection for PaperQA)
# Default: ./papers
PAPER_LIBRARY_DIR=./papers

# ============================================================================
# PubMed Configuration (for internet search)
# ============================================================================

# PubMed Email (recommended for API access)
# PUBMED_EMAIL=your.email@example.com

# PubMed API Key (optional, increases rate limits)
# PUBMED_API_KEY=your_pubmed_api_key

# ============================================================================
# PaperQA Configuration (Now uses OpenRouter by default!)
# ============================================================================

# LLM Model for PaperQA (default: openrouter/google/gemini-2.0-flash-exp:free)
# OpenRouter options (uses OPENROUTER_API_KEY):
#   PAPERQA_LLM=openrouter/google/gemini-2.0-flash-exp:free
#   PAPERQA_LLM=openrouter/anthropic/claude-3.5-sonnet
#   PAPERQA_LLM=openrouter/meta-llama/llama-3.3-70b-instruct
# Direct provider options (require provider-specific API keys):
# PAPERQA_LLM=openrouter/gpt-4.1-mini
# Changed to use the same PAID model as main agents (gemini-3-pro-preview)
PAPERQA_LLM=openrouter/google/gemini-3-pro-preview
#  PAPERQA_LLM=anthropic/claude-sonnet-4-20250514
# PAPERQA_LLM=openrouter/google/gemini-2.0-flash-exp:free

# Embedding Model for PaperQA (default: openrouter/openai/text-embedding-3-small)
# OpenRouter options (uses OPENROUTER_API_KEY):
#   PAPERQA_EMBEDDING=openrouter/openai/text-embedding-3-small
#   PAPERQA_EMBEDDING=openrouter/openai/text-embedding-3-large
# NOTE: For embeddings via OpenRouter, we use the 'openai/' prefix but configure
# the base URL and API key in src/tools/implementations.py to point to OpenRouter.
# This avoids LiteLLM "Unmapped LLM provider" errors for embeddings.
# PAPERQA_EMBEDDING=openai/text-embedding-3-small
# Local option (free, no API key needed, requires: pip install paper-qa[local]):
PAPERQA_EMBEDDING=st-multi-qa-MiniLM-L6-cos-v1
# Direct OpenAI option (requires OPENAI_API_KEY):
#   PAPERQA_EMBEDDING=text-embedding-3-small
# PAPERQA_EMBEDDING=openrouter/openai/text-embedding-3-small

# Maximum source contexts to retrieve (default: 5)
# PAPERQA_MAX_SOURCES=5
